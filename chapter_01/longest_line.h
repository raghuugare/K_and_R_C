#define MAX_LINE_SIZE 1000 /* max input line size. */

int max; /* max line length seen so far. */
char longest_line[MAX_LINE_SIZE]; /* the longest line encountered. */
char line[MAX_LINE_SIZE]; /* the most recently read line... */
